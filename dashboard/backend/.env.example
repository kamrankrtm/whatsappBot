# Server Configuration
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/whatsapp-multi-bot

# JWT Secret for Authentication
JWT_SECRET=your_secret_key_change_this_in_production

# WhatsApp Configuration
# Set to 'false' during development to see WhatsApp browser UI
HEADLESS=true

# Liara Headless Chrome Configuration (optional)
# Uncomment and set these if you want to use remote Headless Chrome
# USE_REMOTE_CHROME=true
# CHROME_WS_ENDPOINT=wss://your-chrome-app-url.liara.run
# CHROME_TOKEN=your-token-here 